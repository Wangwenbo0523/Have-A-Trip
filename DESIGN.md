# Have-A-Trip 设计系统

这份文档描述本站界面**实际在用的**设计语言，是改界面前的第一读物。

选型来自 [VoltAgent/awesome-design-md](https://github.com/VoltAgent/awesome-design-md)
的 Airbnb 一份（`design-md/airbnb/DESIGN.md`，MIT 许可）。选它的理由很直接：本站是一个
**旅游目录**，而 Airbnb 那套是为「以照片为主的消费级旅游市场」设计的 —— 白画布、单一品牌色、
柔和圆角，把视觉重量全部让给照片。库里 1385 个景点、130 张实景照片，缺的正是这样一个画布。

> 商标与品牌资产归 Airbnb 各自所有者；这里只借用**公开描述的设计语言**（配色关系、圆角刻度、
> 层级策略），没有使用对方的字体文件、图标、图片或字标。Airbnb Cereal VF / Circular 是对方
> 自有字体，本站不引用，按上游文档指定的公开替代走 Inter。

## 三层令牌

改颜色只改第一层，第二层自动跟着走；组件样式一律只用第二层 —— 这样换主题不用动组件。

```
原生色板        分块取色              组件
--canvas   →   --page-bg         →   .attractionCard { background: var(--panel-bg) }
--ink      →   --text
--primary  →   --accent
```

令牌定义全部在 `frontend/src/index.css`：`:root` 是浅色（默认），
`:root[data-theme="dark"]` 只覆盖**原生色板**，所以深色主题不需要重写任何组件样式。

### 颜色

| 用途 | 令牌 | 浅色 | 深色 |
|---|---|---|---|
| 画布 | `--canvas` | `#ffffff` | `#101012` |
| 浅一档的实底 | `--surface-soft` | `#f7f7f7` | `#1c1c1f` |
| 再实一档 | `--surface-strong` | `#f2f2f2` | `#26262a` |
| 墨色（标题与正文） | `--ink` | `#222222` | `#f5f5f5` |
| 次要正文 | `--body` | `#3f3f3f` | `rgba(255,255,255,.82)` |
| 弱化文字 | `--muted-ink` | `#6a6a6a` | `#a3a3a8` |
| 细线 | `--hairline` | `#dddddd` | `#2f2f33` |
| 品牌色 Rausch | `--primary` | `#ff385c` | `#ff385c` |
| 按压态 | `--primary-active` | `#e00b41` | `#ff5c78` |
| 错误 | `--error-ink` | `#c13515` | `#ff7a6b` |

**整站只有一处品牌色。** 它出现在：主按钮（`浏览全部景点`、`找一下`、`问问看`、重试）、
页头品牌名、5A 徽章。除此之外页面是白的加黑的。筛选选中态用**墨色实底**而不是红色 ——
一屏上同时有好几组筛选，全刷成红色等于没有重点。

压在照片上的小角标（分类名、评级徽章）取 `--media-chip-bg` / `--media-chip-text`，
这两枚**不随主题走**：底下是照片，深色主题下也应该是白底墨字，否则黑块压在亮图上显脏。
这与 `--surface-overlay`（永远是深色遮罩）是同一条约定。

### 圆角

`--radius-sm: 8px`（按钮、次级按钮、小图）· `--radius-md: 14px`（卡片、面板、输入区）·
`--radius-lg: 20px`（大浮层）· `--radius-xl: 32px` · `--radius-full: 999px`（搜索栏、胶囊、头像）。

除页面栅格本身外**没有硬角**：能点的东西都是圆的。

### 字体与字号

`--font-sans`：`Inter` → 系统字体 → `PingFang SC` / `Microsoft YaHei`。

| 层级 | 字号 / 字重 | 用在哪 |
|---|---|---|
| 页面标题 | 28px / 700 | 首页 h1、`.page__title`、详情页景点名 |
| 分节标题 | 22px / 600 | `.section__title` |
| 卡片标题 | 16px / 600 | `.attractionCard__name` |
| 正文 | 16px / 400 / 1.5 | 长文本 |
| 次要文字 | 14px / 400 | 卡片元信息、地址、票价 |
| 小字 | 13px / 400 | 页头副标题、卡片英文名 |
| 徽章 | 11px / 600 | 评级、分类角标 |

**显示字号是克制的**：首页 h1 只有 28px/700。视觉重量交给照片和卡片，字号一放大就变成
SaaS 落地页的口气，不像一个「去处」的目录。全站唯一一个靠字号撑起来的地方是详情页的评分。

### 层级

**只有一档阴影**，写在 `--shadow-card` 里，用于三处：卡片与分类卡悬停、页头小胶囊悬停、
搜索框静置、浮层。其余全是平的。深度靠照片、白底压白底的细线分块、圆角裁切来表达，
不靠叠加阴影。

## 形状与组件

- **页头**：白底 + 一条细线，吸顶，80px 上下。左品牌（品牌名是 Rausch 色，副标题灰字），
  中间一排文字导航（当前页是**墨色下划线 + 加粗**，不是彩色底），右边语种与主题两个 40px 胶囊。
  用 DOM 顺序 + flex 排版，**不用绝对定位**把工具按钮「挪」到右边。
- **主按钮**：8px 圆角、48px 高、16px/500、Rausch 实底。按压换 `--primary-active`，
  **不降透明度**（白底上降透明度会让红色发灰，看着像被禁用）。
- **胶囊按钮 / 筛选**：`--radius-full`、14px/500。默认空心（白底 + 细线 + 灰字），
  选中换墨色实底。
- **搜索框**：全圆角、白底、一条细线，静置就带那一档阴影；聚焦时线加粗成墨色并左右各减 1px
  顶回内边距，**不发一圈光晕**。
- **卡片**：照片 4:3 打头，14px 圆角，1px 细线，白底。悬停上移 2px 并浮起那一档阴影，
  **不换边框颜色**。分类角标在左上、评级徽章在右上，都是白色小胶囊。
- **评级徽章**：等级越高越响 —— 5A 是 Rausch 实底白字，4A 是墨色实底白字，3A 是白底墨字，
  世界遗产是白底墨字加一圈墨色描边（另一套刻度，不能读成 A 级）。**不发明金色 / 绿色渐变。**
- **空态**：虚线细线框 + 灰字，居中。

## Do / Don't

- **Do** 只用第二层令牌取色，新增组件自动支持两套主题。
- **Do** 改颜色时先问「这是原生色板的哪一格」，改原生值而不是在深色块里重绑。
- **Do** 把照片放大、把字收小；需要强调就给它一块 Rausch。
- **Don't** 新增第二种彩色。金色、青色、渐变按钮都已经删掉了。
- **Don't** 用绝对定位排页头；窄屏会打架。
- **Don't** 给悬停加边框变色；一屏扫过去会到处闪。
- **Don't** 引用 Google Fonts 之类的外链字体（上一版页头引用 Staatliches，已删除）。

## 改完之后

1. `cd frontend; npm test`（157 用例，含 i18n 中英成对与主题默认值）
2. `npm run typecheck`
3. `npm run build` —— **必须重建**：本机应用 `damo-app.ps1` 吃的是 `frontend/dist`，
   不 build 就看不到改动；打包脚本也会因为「产物比源旧」硬失败。
4. `cd ..; python scripts/verify_all.py`