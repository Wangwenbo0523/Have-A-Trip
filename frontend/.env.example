# 复制为 .env.local 后按需修改。.env.local 已在 .gitignore 里, 不要提交。
#
# 留空(或整个变量不写)时前端请求同源的 /api/v1, 开发服务器会把 /api
# 代理到 http://127.0.0.1:8000(见 vite.config.js)。这样本地开发不需要配跨域。
#
# 部署时如果前端与 API 不同源, 指向真实的 API 前缀:
# VITE_API_BASE=https://api.example.com/api/v1
VITE_API_BASE=
